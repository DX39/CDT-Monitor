<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Self-hosted installations may still use the HTTP URL from the server README. HTTPS is recommended for public deployments. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
